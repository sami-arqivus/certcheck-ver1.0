FROM nginx:alpine

# Install bash for envsubst
RUN apk add --no-cache bash

# Copy template
COPY nginx.conf.template /etc/nginx/conf.d/default.template
COPY cert.pem /etc/nginx/ssl/cert.pem
COPY key.pem /etc/nginx/ssl/key.pem
